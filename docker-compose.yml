version: "3.9"

services:
  gridedge:
    build: .
    container_name: gridedge_app
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./logs:/app/logs
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
